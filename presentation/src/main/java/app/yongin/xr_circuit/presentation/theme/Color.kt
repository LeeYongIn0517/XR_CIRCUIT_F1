package app.yongin.xr_circuit.presentation.theme

import androidx.compose.ui.graphics.Color

// Figma Circuit Profile (node 1:12) → Material 3 roles

/** Stats values, Lap Record title — Figma `#2563EB` */
val Blue600 = Color(0xFF2563EB)

/** Selected corner fill, idle turn codes — Figma `#3B82F6` */
val Blue500 = Color(0xFF3B82F6)

/** Lap Record time — Figma `#1D4ED8` */
val Blue700 = Color(0xFF1D4ED8)

/** Lap Record card fill — Figma `#EFF6FF` */
val Blue50 = Color(0xFFEFF6FF)

/** Lap Record card border — Figma `#BFDBFE` */
val Blue200 = Color(0xFFBFDBFE)

/** Panel / card base — Figma white glass */
val SurfaceWhite = Color(0xFFFFFFFF)

/** Title text — Figma `#0F172A` */
val Slate900 = Color(0xFF0F172A)

/** Corner names — Figma `#334155` */
val Slate700 = Color(0xFF334155)

/** Driver / body muted — Figma `#475569` */
val Slate600 = Color(0xFF475569)

/** Stat labels — Figma `#64748B` */
val Slate500 = Color(0xFF64748B)

/** Section headers / year — Figma `#94A3B8` */
val Slate400 = Color(0xFF94A3B8)

/** Stat card border — Figma `#E2E8F0` */
val Slate200 = Color(0xFFE2E8F0)

/** Preview / page backdrop */
val Slate100 = Color(0xFFF1F5F9)

// Dark-theme companions (tonal equivalents, same brand hue)
val Blue80 = Color(0xFFADC6FF)
val Blue20 = Color(0xFF001A41)
val Blue30 = Color(0xFF002E69)
val Blue90 = Color(0xFFD8E2FF)
val Slate80 = Color(0xFFC2C7D0)
val DarkSurface = Color(0xFF1A1C1E)
val DarkOnSurface = Color(0xFFE2E2E6)
val DarkOutline = Color(0xFF8C9198)
val DarkOutlineVariant = Color(0xFF42474E)
